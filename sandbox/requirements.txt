# Python deps for tools/anonymize/ and tools/verify/.
# Kept minimal and pure-Python where possible.
# Hashes are not pinned at this stage; once tools/ stabilizes, regenerate with
# `pip-compile --generate-hashes` and switch bootstrap.sh to --require-hashes.

lxml>=6.1.1,<7           # XML parsing for OPNsense config.xml
ruamel.yaml>=0.19.1,<0.20  # round-trip YAML for Omada/Proxmox configs
detect-secrets>=1.5,<2 # used by the verify pre-commit hook
pre-commit>=4.6.0,<5
